[tools]
node = "26"
just = "latest"

[env]
# Loads variables from a local .env file (gitignored). Copy .env.example to .env.
_.file = ".env"

[settings]
# Auto-install missing tools when entering the directory.
experimental = true
