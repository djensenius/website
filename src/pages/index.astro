---
// Landing shell (issue #34). The modernized site opens on the in-browser Linux
// emulator — the signature terminal experience — with light branding and a link
// to the plain file-navigation view. A proper emulator↔file-nav toggle comes in
// issue #35.
import Emulator from '../components/Emulator.astro';
import ModeToggle from '../components/ModeToggle.astro';
const title = 'David Jensenius';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta
      name="description"
      content="David Jensenius composer and media artist."
    />
  </head>
  <body>
    <div class="shell">
      <header class="masthead">
        <div class="titlebar">
          <h1>{title}</h1>
          <ModeToggle active="terminal" />
        </div>
        <p class="tagline">
          Composer and media artist. Switch to <strong>Files</strong> above for plain pages.
        </p>
      </header>
      <main class="stage">
        <Emulator />
      </main>
    </div>

    <style>
      :root {
        color-scheme: light dark;
        --shell-bg: #0b0b0b;
        --shell-fg: #d7d7d7;
        --shell-muted: #8a8a8a;
        --shell-link: #7aa2f7;
      }

      @media (prefers-color-scheme: light) {
        :root {
          --shell-bg: #fbfbfa;
          --shell-fg: #1a1a1a;
          --shell-muted: #6a6a6a;
          --shell-link: #1d4ed8;
        }
      }

      html,
      body {
        height: 100%;
      }

      body {
        margin: 0;
        background: var(--shell-bg);
        color: var(--shell-fg);
        font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
      }

      /* Full-viewport column: a compact header with my info, then the emulator
       * filling all remaining height. */
      .shell {
        display: flex;
        flex-direction: column;
        height: 100vh;
        height: 100dvh;
        padding: 1rem 1rem 1.25rem;
        box-sizing: border-box;
      }

      .masthead {
        flex: 0 0 auto;
        margin-bottom: 0.75rem;
      }

      .titlebar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
      }

      .masthead h1 {
        margin: 0 0 0.25rem;
        font-size: 1.5rem;
      }

      .tagline {
        margin: 0;
        max-width: 46rem;
        color: var(--shell-muted);
        line-height: 1.5;
        font-size: 0.9rem;
      }

      .tagline a {
        color: var(--shell-link);
      }

      /* The emulator fills the leftover vertical space. */
      .stage {
        flex: 1 1 auto;
        min-height: 0;
        display: flex;
      }
    </style>
  </body>
</html>
